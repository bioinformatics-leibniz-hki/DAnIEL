#!/usr/bin/env bioawk

# Clean FASTQ
# - Remove comments in header
# - Remove barcodes in header to allow pair matching
# - Ensure that every third line is only '+' (NCBi SRA writes the id there which PIPITS denies)

{
	name=gsub(/#[ATGC_]+/, "#", $name);
	print "@"$name"\n"$seq"\n+\n"$qual
}